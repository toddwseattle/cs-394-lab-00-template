import './BioCard.css';

import React from 'react';

// TODO: Define the BioCardProps interface here
// Hint: Check the README.md for the interface structure
// What properties should a bio card accept?

// TODO: Update the function signature to accept props
// Hint: Use destructuring in the function parameters
// Example pattern: function BioCard({ prop1, prop2 }: PropsInterface)

export function BioCard() {
  // TODO: Implement the BioCard component
  // Hint: Before you start, run the tests to see what they expect!
  // Steps to consider:
  // 1. Look at tests/biocard.test.tsx to understand requirements
  // 2. The component should render:
  //    - An image with src and alt attributes
  //    - A link (anchor tag) with the name
  //    - A description paragraph
  // 3. Use the className "bio-card" for the container div
  // 4. Reference the props you defined in the interface

  return (
    <div className="bio-card">
      <p>No BioCard Implemented</p>
      {/* Remove this placeholder and add your implementation */}
      {/* Hint: Check what the tests expect to see rendered */}
    </div>
  );
}
